<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="dev.mvc.stock.stockDAOInter">
<insert id = "stock_create" parameterType = "dev.mvc.stock.stockVO">
    INSERT INTO stock(stocknum, stockno, productclass, productwa, productst, productno )
     VALUES (stocknum_seq.nextval, #{stockno},#{productclass},#{productwa},#{productst},#{productno})
</insert>

<select id="stock_list"  resultType="dev.mvc.stock.stockVO">
    SELECT stocknum, stockno, productclass, productwa, productst, productno
    FROM stock 
    ORDER BY stocknum ASC
</select>

<select id="stock_read"  parameterType = "int" resultType="dev.mvc.stock.stockVO">
    SELECT stocknum, stockno, productclass, productwa, productst, productno
    FROM stock 
    WHERE stocknum = #{stocknum}
</select>

<update id="stock_update"  parameterType="dev.mvc.stock.stockVO">
    UPDATE stock
    SET stockno=#{stockno}, productclass=#{productclass}, productwa=#{productwa}, productst=#{productst}, productno=#{productno}
    WHERE stocknum=#{stocknum}
</update>

<delete id="stock_delete" parameterType = "int">
    DELETE FROM stock 
    WHERE stocknum=#{stocknum}
</delete>

</mapper>